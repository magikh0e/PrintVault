{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. Filesystem access goes through this app's own pv_* commands rather than a general filesystem plugin, so no broad fs permission is granted.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
